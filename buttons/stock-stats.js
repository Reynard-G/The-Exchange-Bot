const { EmbedBuilder } = require("discord.js");

module.exports = {
  id: "stock:stats",
  permissions: [],
  run: async (client, interaction) => {
    // Today's Open

    // Today's High

    // Today's Low

    // Market Cap

    // P/E Ratio
  }
};